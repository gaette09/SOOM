# Queue Map Check

Purpose: verify that `docs/ops/TODAY_QUEUE.md` maps each Active task to an existing task file and report file.

Required checks:

- SOOM Active task exists.
- JAFOM Active task exists.
- Instagram Active task exists.
- Each Active task is present in `docs/ops/PROJECT_GOALS.md`.
- Each Active task is present in `docs/ops/TODAY_QUEUE.md`.
- Each Active task has a matching report in `docs/reports/`.

